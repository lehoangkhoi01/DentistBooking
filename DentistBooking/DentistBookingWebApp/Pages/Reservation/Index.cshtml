@page
@model DentistBookingWebApp.Pages.Reservation.IndexModel
@{
    ViewData["Title"] = "Reservation";
}

<div class="reservation-wrapper px-5 py-5 my-3">
    <div class="form-row col-12 reservation-top pb-4">
        <div class="col-12 col-lg-4 left-section my-3">
            @*<h4>Dentist Option</h4>
            <button class="col-6 btn btn-success my-3" onclick="onClickChooseAutoDentist()">Auto choose dentist</button>
            <button type="button"
                    data-toggle="modal"
                    data-target="#dentistModal"
                    class="col-6 btn btn-primary my-3">
                Choose your dentist
            </button>
            <h5>Dentist: <span id="dentistName"></span> </h5>*@
        </div>

        <div class="col-12 col-lg-8 right-section">
            <div class="container-fluid px-0 px-sm-4 mx-auto">
                <div class="col-12">
                    <form autocomplete="off" method="post" id="reservationForm">
                        <div class="form-group col-lg-6 mb-4 service-choose">
                            <label class="control-label">Choose a service</label>
                            <select class="form-control" asp-items="ViewBag.Service" id="serviceSelectBox"></select>
                        </div>
                        <div class="card border-0">
                            <div class="card-header bg-primary">
                                <div class="mx-0 mb-0 row justify-content-sm-center justify-content-start px-1">
                                    <input onchange="onChangeDatePicker()" type="text" id="date-picker" class="datepicker" placeholder="Pick Date" name="dateReservation" readonly>
                                    <span class="fa fa-calendar"></span>
                                </div>
                            </div>

                            <div class="card-body p-3 p-sm-5">
                                <div class="row text-center mx-0 col-12">
                                    <div class="col-12 col-lg-6">
                                        <label class="control-label">Time</label>
                                        <select class="form-control" asp-items="ViewBag.TimeList" id="timeSelectBox" onchange="onChangeTimeSelectBox()"></select>
                                    </div>

                                    @*@foreach (var time in ViewBag.TimeList)
                                    {
                                    <div class="col-md-2 col-4 my-1 px-2">
                                    <div class="cell py-1">@time</div>
                                    </div>
                                    }*@
                                    <div class="text-center mx-0 col-12 col-lg-6">
                                        <label class="control-label">Dentist Options</label>
                                        <select id="chosenDentist" class="form-control mb-3" asp-items="ViewBag.DentistList" onchange="onChangeDentistSelectBox()"></select>
                                        <button type="button" class="col-12 btn btn-success" onclick="onClickChooseAutoDentist()">Auto choose dentist</button>
                                    </div>
                                </div>

                            </div>
                        </div>
                        <input name="patientId" id="chosenPatientId" hidden />
                        <input name="timeReservation" id="timeReservation" hidden />
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="text-center">
        <h5>Detail</h5>
        <h6>Date & Time: <span id="datetimeDetail"></span></h6>
        <h6>Dentist: <span id="dentistDetail"></span></h6>
    </div>
    <div class="reservation-bottom col-12">
        <div class="text-center">
            <button type="button" class="btn btn-outline-success col-lg-4">Complete</button>
        </div>
    </div>
</div>


<!-- Hidden form to request load dentist list-->
<form method="post" asp-page-handler="LoadDentist" id="loadDentistForm">
    <input type="hidden" name="date" id="dateInput" />
    <input type="hidden" name="time" id="timeInput" />
</form>


<!-- Choose Dentist Modal -->
<div class="modal fade" id="dentistModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLongTitle">Dentist list</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="form-group col-12">
                    <select id="chosenDentist" class="form-control" asp-items="ViewBag.DentistList"></select>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" onclick="onClickChooseDentist()" class="btn btn-outline-primary">Choose</button>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <link href="@Url.Content("~/css/reservation/index.css")" rel="stylesheet" type="text/css" />
}

    @section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.4.1/js/bootstrap.js"></script>
    <script>
        $(document).ready(function() {
            $('.datepicker').datepicker({
                format: 'dd-mm-yyyy',
                autoclose: true,
                startDate: '0d'
            });
            var date = moment();
            var currentDate = date.format('DD-MM-YYYY');
            if('@ViewBag.Date' != "") {
                $("#date-picker").val('@ViewBag.Date');
            }
            else $("#date-picker").val(currentDate);

            var date = $("#date-picker").val();
            var time = $("#timeSelectBox").val();
            var dateTimeString = date + " " + time;
            var dentistId = $("#chosenDentist").val();
            var dentistName = $("#chosenDentist option:selected").text();
            $("#datetimeDetail").text(dateTimeString);
            $("#dentistDetail").text(dentistName);
            $("#timeInput").val(time);
            $("#dateInput").val(date);

            $('.cell').click(function() {
                $('.cell').removeClass('select');
                $(this).addClass('select');
            });
        });
    </script>
    <script>
        //function onClickChooseDentist() {
        //    $("#dentistModal").modal('hide');
        //    var dentistId = $("#chosenDentist").val();
        //    var dentistName = $("#chosenDentist option:selected").text();
        //    $("#dentistDetail").text(dentistName);
        //}

        function onClickChooseAutoDentist() {
            $("#dentistDetail").text("Auto choose");
        }

        function onChangeTimeSelectBox() {
            var time = $("#timeSelectBox").val();
            var date = $("#date-picker").val();
            $("#timeInput").val(time);
            $("#dateInput").val(date);

            var date = $("#date-picker").val();
            var time = $("#timeSelectBox").val();
            var dateTimeString = date + " " + time;
            $("#datetimeDetail").text(dateTimeString);

            $("#loadDentistForm").submit();
        }

        function onChangeDatePicker() {
            var date = $("#date-picker").val();
            var time = $("#timeSelectBox").val();
            var dateTimeString = date + " " + time;
            $("#datetimeDetail").text(dateTimeString);
            $("#timeInput").val(time);
            $("#dateInput").val(date);

            $("#loadDentistForm").submit();
        }

        function onChangeDentistSelectBox() {
            var dentistId = $("#chosenDentist").val();
            var dentistName = $("#chosenDentist option:selected").text();
            $("#dentistDetail").text(dentistName);            
        }
    </script>
}