@page
@model DentistBookingWebApp.Pages.Services.DetailsModel
@{
    ViewData["Title"] = "Detail";
}


@*<h1>Details</h1>

<div>
    <h4>Service</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.serviceViewModel.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.serviceViewModel.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.serviceViewModel.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.serviceViewModel.Description)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.serviceViewModel.Price)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.serviceViewModel.Price)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.serviceViewModel.Image)
        </dt>
        <dd class="col-sm-10">
            <img src="~/images/@Model.serviceViewModel.Image" alt="@Model.serviceViewModel.Image" height="200" width="200" />
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.serviceViewModel.Status)
        </dt>
        <dd class="col-sm-10">
            @if (Model.serviceViewModel.Status)
            {
                <span>Active</span>
            }
            else
            {
                <span>Inactive</span>
            }
        </dd>
    </dl>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
    @if (!string.IsNullOrEmpty(Model.Role) && Model.Role == "1")
    {
        <a asp-page="./Edit" asp-route-id="@Model.serviceViewModel.Id">Edit |</a>
        <form method="post" asp-page-handler="Delete">
            <input name="serviceId" value="@Model.serviceViewModel.Id" type="hidden" />
            <button type="submit" class="btn btn-danger">Delete</button>
        </form>
    }
    else
    {
        <a href="#">Booking appoitment</a>
    }
</div>*@


<div class="container">
    <div class="body-top mt-3">
        <div class="service-name text-center mb-4">
            <h2>@Model.serviceViewModel.Name</h2>
        </div>
    </div>

@*    <div class="specialty-section col-12 mb-3">
        <span style="font-weight: bold" class="mr-1">Specialty:</span>@Model.Specialty.SpecialtyName
    </div>*@
    <div class="row mb-5">
        <div class="body-left col-12">
            <div>
                <div class="service-thumb mb-3">
                    <img src="~/images/@Model.serviceViewModel.Image" />
                </div>
                <div class="service-des">
                    @Html.Raw(Model.serviceViewModel.Description)
                </div>
                <div class="price text-center float-right">
                    <span style="font-weight: bold">Price:</span> $@Model.serviceViewModel.Price
                </div>
            </div>
            <div class="funct-button text-center mt-5">
                @if(!string.IsNullOrEmpty(Model.Role) && Model.Role == "1")
                {
                    <a class="btn btn-success col-4"
                       asp-page="/Services/Edit"
                       asp-route-id="@Model.serviceViewModel.Id">
                        Edit
                    </a><br>
                }
                else
                {
                    <a class="btn btn-success col-4"
                       href="#">
                        Make reservation
                    </a><br>
                }

            </div>
        </div>
    </div>
</div>

@section Styles {
    <link href="@Url.Content("~/css/service/detail.css")" rel="stylesheet" type="text/css" />
}

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}